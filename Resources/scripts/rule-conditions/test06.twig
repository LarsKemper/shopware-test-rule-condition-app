{% if scope.cart is not defined %}
    {% return false %}
{% endif %}

{% for lineItem in scope.cart.lineItems.getFlat() %}
    {% if compare('=', compare('=', lineItem.type, 'product'), expected) %}
        {% return true %}
    {% endif %}
{% endfor %}

{% return false %}
