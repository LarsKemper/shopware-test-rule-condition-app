{% if scope.cart is not defined %}
    {% return false %}
{% endif %}

{% for lineItem in scope.cart.lineItems.getFlat() %}
    {% if compare(operator, lineItem.type, multiselection) %}
        {% return true %}
    {% endif %}
{% endfor %}

{% return false %}
