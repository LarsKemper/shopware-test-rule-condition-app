{% if scope.cart is not defined %}
    {% return false %}
{% endif %}

{% for lineItem in scope.cart.lineItems.getFlat() %}
    {% if compare(operator, date(lineItem.getPayloadValue('createdAt')), date(datetime)) %}
        {% return true %}
    {% endif %}
{% endfor %}

{% return false %}
