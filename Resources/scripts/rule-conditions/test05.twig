{% if scope.cart is not defined %}
    {% return false %}
{% endif %}

{% for lineItem in scope.cart.lineItems.getFlat() %}
    {% if compare(operator, lineItem.referencedId, productId) %}
        {% return true %}
    {% endif %}
{% endfor %}

{% return false %}
