{% if scope.salesChannelContext.customer is not defined %}
    {% return false %}
{% endif %}

{% do debug.dump(scope.salesChannelContext.customer.groupId) %}

{% return compare(operator, scope.salesChannelContext.customer.groupId, customerGroupIds) %}
